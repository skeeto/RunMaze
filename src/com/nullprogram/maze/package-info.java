/**
 * A very simple animated maze solver applet, which can also be run
 * standalone. It's build on a generic maze implementations so that
 * various types of mazes can be implemented without having to modify
 * the solver or display code.
 */
